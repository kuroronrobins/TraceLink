# 07 実装開始ガイド

## Android

1. `PostgresGateway` の実装方針を決める。
2. `WorkContextRepository`、`RuleRepository`、`EquipmentMasterRepository` の fake 実装を追加する。
3. `ReadJudgementService` の純粋実装を追加する。
4. `ReadResultRepository` の PostgreSQL 実装を追加する。
5. `PendingWriteQueue` を durable storage へ差し替える。
6. `DefaultInventoryRepository` に依存を注入し、処理順を図と同じにする。

## PostgreSQL

1. Android 用 View / Function 名を確定する。
2. function 入出力を fixture で確認する。
3. 制約と audit を入れる。
4. XCgate 用 final result view を作る。

## テスト

- session 内重複除去。
- local judgement。
- registration function 成功。
- registration function 失敗。
- pending write 再実行。
- UI が repository state だけで表示されること。
